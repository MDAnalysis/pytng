name: pytng
channels:
  - conda-forge
  - defaults
dependencies:
  - numpy
  - cython
  - zlib
  - sphinx
  - pytest
  - pytest-cov
  - pip
  # docs
  - pip:
    - sphinx-sitemap
    - mdanalysis-sphinx-theme
